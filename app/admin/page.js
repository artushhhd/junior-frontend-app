import AdminDashboard from './admin';

export const metadata = {
    title: 'Admin Management Board',
    description: 'System moderation tools',
};

export default function AdminPage() {
    return <AdminDashboard />;
}